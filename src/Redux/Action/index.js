export const
    setInputTitle= (title) => ({
      type: "SET_INPUT_TITLE",
      title,
    }) 
    export const
    createBlog= (blog) => ({
      type: "CREATE_BLOG",
      blog,
    }) 
    export const
    setReset= () => ({
      type: "RESET_INPUT"
    })
